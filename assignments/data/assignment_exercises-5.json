{
  "instructor": "Phil Sinatra",
  "class_id": "ID221",
  "assignment_name": "Assignment 5: CSS Box Model",
  "exercises": [
    {
      "instructions": "<pre class=\"prettyprint\">div {&NewLine;  border: 5px solid blue;&NewLine;  margin: 32px;&NewLine;  padding: 16px;&NewLine;  width: 900px;&NewLine;}</pre><p>Based on the CSS, what is the width of the <code>div</code> in pixels?</p>",
      "type": "input_text",
      "placeholder": "#px",
      "solution":"1006px"
    },
    {
      "instructions": "<p>Program a CSS selector that sets the size of all <code>div</code> elements to <code>900px wide</code> x <code>450px</code> tall. (<b>Note:</b> program your selectors in alphabetical order)</p>",
      "type": "textarea",
      "rows": "4",
      "solution":"div { height: 450px; width: 900px; }"
    },
    {
      "instructions": "<p>Here is some CSS:</p> <pre class=\"prettyprint\">html {&NewLine;  font-size: 100%;&NewLine;}&NewLine;&NewLine;div {&NewLine;  font-size: 1rem;&NewLine;}</pre> <p>Based on the provided styles, program a new CSS selector that adds a <b>32px</b> <code>margin</code> to the <b>top</b> and <b>bottom</b> of every <code>div</code> element, <b>BUT</b> your <code>margin</code> value should be expressed in <code>rem</code> units, not <code>pixels</code>, and should be done using <b>shorthand</b> notation.</p>",
      "type": "textarea",
      "rows": "3",
      "solution":"div { margin: 2rem 0; }"
    },
    {
      "instructions": "<p>Here is some CSS:</p> <pre class=\"prettyprint\">html {&NewLine;  font-size: 100%;&NewLine;}&NewLine;&NewLine;div {&NewLine;  font-size: 1rem;&NewLine;}</pre> <p>Based on the provided styles, program a new CSS selector that adds <b>48px</b> of <code>padding</code> to the <b>all four sides</b> of every <code>div</code> element that has a <code>class</code> equal to <b>either</b> <code>intro</code> or <code>sidebar</code>. Your <code>padding</code> value should be expressed in <code>rem</code> units, not <code>pixels</code>, and should be done using <b>shorthand</b> notation.</p>",
      "type": "textarea",
      "rows": "4",
      "solution":"div.intro, div.sidebar { padding: 3rem; }"
    },
    {
      "instructions": "<pre class=\"prettyprint\">* { box-sizing: border-box; }&NewLine;div {&NewLine;  border: 5px solid blue;&NewLine;  margin: 32px;&NewLine;  padding: 16px;&NewLine;  width: 900px;&NewLine;}</pre><p>Based on the CSS, what is the width of the <code>div</code> in pixels?</p>",
      "type": "input_text",
      "placeholder": "#px",
      "solution":"900px"
    },
    {
      "instructions": "<p>Program a CSS selector that adds a solid, <span style=\"border: 2px solid #2edb00;\">2px border</span> to all <code>span</code> elements. The border color should be hex code <span style=\"color: #2edb00; \">2edb00</span> (<b>Note</b>: your selector should be written using <b>shorthand</b> notation)</p>",
      "type": "textarea",
      "rows": "3",
      "solution":"span { border: 2px solid #2edb00; }"
    },
    {
      "instructions": "<p>Program a CSS selector with the following declarations: (<b>Note</b>: write all of your selectors in alphabetical order, and use <b>shorthand</b> notation wherever possible)</p> <ul> <li>element being selected is a <code>header</code></li> <li>element being selected <code>class</code> should be equal to <i>header</i></li> <li>text color should be hexcode <span style=\"color: #ffc700\">ffc700</span></li> <li>element background color should be hexcode <span style=\"background-color: #18354a; color:#ffc700;\">18354a</span></li> <li>text font size should be equivalent to 24px</li> <li>element should have a 32px margin on the top and bottom</li> <li>element should be horizontally centered</li> <li>element should have 32px of padding on all four sides</li> <li>element should have a <span style=\"border: 1px solid #ffc700\"> solid, 1px border, color is hexcode ffc700</span></li> <li>element should be as wide as possible, but no wider than 1100px</li> <li>element should be as high/tall as necessary based on the content within</li> <li>all size/dimension values should be coded in <code>rem</code> units (assume root element <code>font-size: 100%</code>)</li> </ul>",
      "type": "textarea",
      "rows": "9",
      "solution":"header.header { background-color: #18354a; border: 1px solid #ffc700; color: #ffc700; font-size: 1.5rem; margin: 2rem auto; max-width: 68.75rem; padding: 2rem; }"
    },
    {
      "instructions": "<p>Program a CSS declaration for the <code>margin</code> property. The value should be based on the following:</p><ul><li>bottom: 16px</li><li>top: 24px</li><li>left: 16px</li><li>right: 16px</li></ul><p>All size/dimension values should be coded in <code>rem</code> units (assume root element <code>font-size: 100%</code>). The value should be coded in the shortest possible style.</p>",
      "type": "textarea",
      "rows": "1",
      "solution":"margin: 1.5rem 1rem 1rem;"
    },
    {
      "instructions": "<p>Program a CSS declaration for the <code>padding</code> property. The value should be based on the following:</p><ul><li>bottom: 24px</li><li>top: 24px</li><li>left: 24px</li><li>right: 32px</li></ul><p>All size/dimension values should be coded in <code>rem</code> units (assume root element <code>font-size: 100%</code>). The value should be coded in the shortest possible style.</p>",
      "type": "textarea",
      "rows": "1",
      "solution":"padding: 1.5rem 1.5rem 1.5rem 2rem"
    },
    {
      "instructions": "<pre class=\"prettyprint\">div { box-sizing: border-box; }&NewLine;section {&NewLine;  border: 5px solid blue;&NewLine;  margin: 32px;&NewLine;  padding: 16px;&NewLine;  width: 900px;&NewLine;}</pre><p>Based on the CSS, what is the width of the <code>section</code> in pixels?</p>",
      "type": "input_text",
      "placeholder": "#px",
      "solution":"1006px"
    },
    {
      "instructions": "<div style=\"margin-bottom: .5rem; padding: 1rem; border: 4px solid; border-color: red purple blue green;\"><p>Use CSS to program the styles for a <code>div</code> so that there is a solid, 2px border around the <code>div</code> with the following colors:</p><ul><li>top: red</li><li>bottom: blue</li><li>left: green</li><li>right: purple</li></ul><p>You should program this solution in the least amount of code possible. (HINT: should be three lines of code)</p></div>",
      "type": "textarea",
      "rows": "3",
      "solution":"border-color: red purple blue green; border-style: solid; border-width: 2px;"
    },
    {
      "instructions": "<p>Program a CSS ruleset for a <code>div</code> with the following requirements met:</p><ul><li>the selector should target only <code>div</code> elements that have a class that includes \"wrapper\".</li><li>The <code>div</code> should be as wide as possible, but no wider than 1000px</li><li>The <code>div</code> should be horizontally centered on the screen.</li><li>The <code>div</code> should have a 48px margin on the top and bottom.</li><li>All size/dimension values should be coded in <code>rem</code> units (assume root element <code>font-size: 100%</code>)</li></ul>",
      "type": "textarea",
      "rows": "3",
      "solution":"div.wrapper { margin: 3rem; max-width: 62.5rem; }"
    },
    {
      "instructions": "<p>Program the <code>height</code> and <code>width</code> values for a container that match the screen size of a user's device.</p>",
      "type": "textarea",
      "rows": "2",
      "solution":"height: 100vh; width: 100vh;"
    },
    {
      "instructions": "<p>I'm tired of doing math. I don't want the browser to add all of the <code>margin</code> and <code>padding</code> values to the overall size of my containers. If I say <code>width: 900px</code>, I want the width to be 900px, even after I add <code>margin</code> and <code>padding</code> styles 😢. Program the CSS that will make it so.</p>",
      "type": "textarea",
      "rows": "3",
      "solution":"* { box-sizing: border-box; }"
    },
    {
      "instructions": "<p>Program a CSS rule that will round all the corners of an element by 16px, but you better not use pixels as your unit of measurment. Assume root element <code>font-size: 100%</code></p>",
      "type": "textarea",
      "rows": "1",
      "solution":"border-radius: 1rem;"
    },
    {
      "instructions": "<p>Program a CSS rule that will change the color of the background of an element to hex code <span style=\"color: #2edb00\">2edb00</span>.</p>",
      "type": "textarea",
      "rows": "1",
      "solution":"background-color: #2edb00;"
    },
    {
      "instructions": "<p>Program a CSS rule that will set a background image for an element to an image file named \"background.png\" that is up one directory from the location of the stylesheet, in a folder named \"images\". This image should not repeat.",
      "type": "textarea",
      "rows": "1",
      "solution":"background-image: url('../images/background.png') no-repeat;"
    },
    {
      "instructions": "<p>Program a CSS declaration for an element with a <code>class</code> attribute that has a value of \"hero\" with the following properties:</p><ul><li>The font size should be four times larger than the parent element font size.</li><li>The text color should be white.</li><li>The background should be an image file named \"hero-1.jpg\", located up two directories from the location of the stylesheet, in a folder named \"ui\" This image should not repeat.</li><li>The background image should be as large as possible so the container is completely covered.</li><li>The container should have a 3rem margin on the bottom.</li><li>The container should have 2rem's of padding on all four sides.</li><li>The container should be centered horizontally.</li><li>The container should have a 5px, black, solid border on all four sides.</li><li>The container should be no wider than 900px</li></ul>",
      "type": "textarea",
      "rows": "8",
      "solution":".hero { background-size: cover; background: url('../../ui/hero-1.jpg') no-repeat; border: 5px solid black; color: white; font-size: 400%; margin: 0 auto 3rem; max-width: 56.25rem; padding: 2rem; }"
    },
    {
      "instructions": "<pre class=\"prettyprint\">* { box-sizing: border-box; }&NewLine;html { font-size: 100%; }&NewLine;div {&NewLine;  border: 1px solid gray;&NewLine;  box-sizing: content-box;&NewLine;  margin: 1.5rem auto 3rem;&NewLine;  max-width: 56.25rem;&NewLine;  padding: 3rem;&NewLine;}&NewLine;div.hero {&NewLine;  border: 5px solid blue;&NewLine;  margin: 3rem auto;&NewLine;  width: 25rem;&NewLine;}</pre><p>What is the final width of <code>.hero</code> in pixels?</p>",
      "type": "input_text",
      "placeholder": "#px",
      "solution":"506px"
    }
  ]
}
